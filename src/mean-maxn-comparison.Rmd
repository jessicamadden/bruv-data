---
title: "Mean MaxN Comparison"
author: "Jessica Madden"
date: "December 16, 2020"
output:
  html_document: default
  pdf_document: default
  word_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)

library(tidyverse)
library(here)
library(wesanderson)
```

```{r}
# Read in point data

point_data_complete <- read_csv(here("data", "point_data_complete.csv"))
five_min <- read_csv(here("data", "point_data_fivemin_complete.csv"))
ten_min <- read_csv(here("data", "point_data_tenmin_complete.csv"))
```
# Mean MaxN through time
### Barred surfperch at Percos and Refugio

```{r}
# Case study: Percos vs Refugio comparison of mean MaxN of barred surfperch
# Get mean maxN for each 5 min interval

perc_ref_five <- five_min %>% 
  filter(site_full %in% c("Percos", "Refugio")) %>%
  filter(common == "Barred surfperch") 

perc_ref_five_summary <- perc_ref_five %>% 
  group_by(op_code, site_full, common, five_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(five_min, site_full) %>% 
  summarize(
    mean_maxn = mean(maxn), 
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

# Get mean maxN for each 10 min interval

perc_ref_ten <- ten_min %>% 
  filter(site_full %in% c("Percos", "Refugio")) %>%
  filter(common == "Barred surfperch") 

perc_ref_ten_summary <- perc_ref_ten %>% 
  group_by(op_code, site_full, common, ten_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(ten_min, site_full) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )
```


```{r, fig.align='center', fig.width=10}
ggplot(perc_ref_ten_summary, aes(x = ten_min, y = mean_maxn, fill = site_full)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = ten_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_x_continuous(limits = c(0, 65), 
                     n.breaks = 6) +
  scale_y_continuous(limits = c(0, 18.5), 
                     n.breaks = 10) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Barred surfperch Mean MaxN", subtitle = "Averaged across deployments for each 10 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```

```{r, fig.align='center', fig.width=10}
ggplot(perc_ref_five_summary, aes(x = five_min, y = mean_maxn, fill = site_full)) +
  geom_bar(stat="identity", 
           position = "dodge") +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = five_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 5)) +
  scale_x_continuous(limits = c(0, 66), 
                     n.breaks = 11) +
  scale_y_continuous(limits = c(0, 18.5), 
                     n.breaks = 10) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Barred surfperch Mean MaxN", subtitle = "Averaged across deployments for each 5 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```


### Leopard sharks at Dume Cove and Leo Carrillo
```{r}
# Case study: Dume vs Leo comparison of mean MaxN of leopard sharks
# Get mean maxN for each 5 min interval

dume_leo_five <- five_min %>% 
  filter(site_full %in% c("Dume Cove", "Leo Carrillo")) %>%
  filter(common == "Leopard shark") %>% 
  group_by(op_code, site_full, common, five_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(five_min, site_full) %>% 
  summarize(
    mean_maxn = mean(maxn), 
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

# Get mean maxN for each 10 min interval

dume_leo_ten <- ten_min %>% 
  filter(site_full %in% c("Dume Cove", "Leo Carrillo")) %>%
  filter(common == "Leopard shark") %>% 
  group_by(op_code, site_full, common, ten_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(ten_min, site_full) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )
```


```{r, fig.align='center', fig.width=10}
ggplot(dume_leo_ten, aes(x = ten_min, y = mean_maxn, fill = site_full)) +
  geom_bar(stat="identity", 
           position = "dodge") +  
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = ten_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_x_continuous(limits = c(0, 65), 
                     n.breaks = 6) +
  scale_y_continuous(limits = c(0, 4),
                     n.breaks = 10) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Leopard shark Mean MaxN", subtitle = "Averaged across deployments for each 10 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```

```{r, fig.align='center', fig.width=10}
ggplot(dume_leo_five, aes(x = five_min, y = mean_maxn, fill = site_full)) +
  geom_bar(stat="identity", 
           position = "dodge") +  
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = five_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 5)) +
  scale_x_continuous(limits = c(0, 65), 
                     n.breaks = 11) +
  scale_y_continuous(limits = c(0, 4), 
                     n.breaks = 5) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Leopard shark Mean MaxN", subtitle = "Averaged across deployments for each 5 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```

### Unknown silversides at South Campus and Haskells

```{r}
# Case study: South Campus vs Haskells comparison of mean MaxN of unknown Atherinid
# Get mean maxN for each 5 min interval

soca_habe_five <- five_min %>% 
  filter(site_full %in% c("South Campus", "Haskells")) %>%
  filter(common == "Unknown Silverside") 

soca_habe_five_summary <- soca_habe_five %>% 
  group_by(op_code, site_full, geog, common, five_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(five_min, site_full, geog) %>% 
  summarize(
    mean_maxn = mean(maxn), 
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  ) %>% 
  arrange(geog, five_min)

# Get mean maxN for each 10 min interval

soca_habe_ten <- ten_min %>% 
  filter(site_full %in% c("South Campus", "Haskells")) %>%
  filter(common == "Unknown Silverside") 

soca_habe_ten_summary <- soca_habe_ten %>% 
  group_by(op_code, site_full, geog, common, ten_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(ten_min, site_full, geog) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  ) %>% 
  arrange(geog, ten_min)
```


```{r, fig.align='center', fig.width=10}
ggplot(soca_habe_ten_summary, aes(x = ten_min, y = mean_maxn, fill = reorder(site_full, geog))) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = ten_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_x_continuous(n.breaks = 6) +
  scale_y_continuous(limits = c(0, 13), 
                     n.breaks = 10) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Unknown silverside Mean MaxN", subtitle = "Averaged across deployments for each 10 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```

```{r, fig.align='center', fig.width=10}
ggplot(soca_habe_five_summary, aes(x = five_min, y = mean_maxn, fill = reorder(site_full, geog))) +
  geom_bar(stat="identity", 
           position = "dodge") +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = five_min, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = 2, 
                alpha = 0.9, 
                position = position_dodge(width = 5)) +
  scale_x_continuous(n.breaks = 11, 
                     limits = c(0, 64)) +
  scale_y_continuous(limits = c(0, 13), 
                     n.breaks = 10) +
  labs(x = "Time (minutes)", y = "Mean MaxN", title = "Unknown silverside Mean MaxN", subtitle = "Averaged across deployments for each 5 min segment of 60 min videos") +
  theme_minimal() +
  theme(legend.title = element_blank())
```

# Mean MaxN across sites

Mean Max N comparison between: 

-  deployments as replicates 
-  5 min time segments as replicates 
-  10 min time segments as replicates for each site

### Barred surfperch at all sites

```{r}
# Each deployment as replicate for each site

barred_all <- point_data_complete %>% 
  filter(common == "Barred surfperch") %>% 
  group_by(op_code, site_full, type, geog, date, bruv, subclass, family, common) %>% 
  summarize(
    maxn = max(number)
  ) %>%
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn), 
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  ) %>% 
  arrange(geog)

# Each 10 min time segment as replicate for each site

barred_ten <- ten_min %>% 
  filter(common == "Barred surfperch") %>% 
  group_by(op_code, site_full, type, geog, common, ten_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

# Each 5 min time segment as replicate for each site

barred_five <- five_min %>% 
  filter(common == "Barred surfperch") %>% 
  group_by(op_code, site_full, type, geog, common, five_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

```

```{r, fig.align='center', fig.width=10}
ggplot(barred_all, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Barred surfperch Mean MaxN", subtitle = "Averaged with each deployment as replicate") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```

```{r, fig.align='center', fig.width=10}
ggplot(barred_ten, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(limits = c(0, 9), 
                     n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Barred surfperch Mean MaxN", subtitle = "Averaged across each 10 minute segment of 60 min deployment") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```

```{r, fig.align='center', fig.width=10}
ggplot(barred_five, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(limits = c(0, 9),
                     n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Barred surfperch Mean MaxN", subtitle = "Averaged across each 5 minute segment of 60 min deployment") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```

#### Yellowfin croaker at all sites

```{r}
# Each deployment as replicate for each site

yf_all <- point_data_complete %>% 
  filter(common == "Yellowfin croaker") %>% 
  group_by(op_code, site_full, type, geog, date, bruv, subclass, family, common) %>% 
  summarize(
    maxn = max(number)
  ) %>%
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn), 
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  ) %>% 
  arrange(geog)

# Each 10 min time segment as replicate for each site

yf_ten <- ten_min %>% 
  filter(common == "Yellowfin croaker") %>% 
  group_by(op_code, site_full, type, geog, common, ten_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

# Each 5 min time segment as replicate for each site

yf_five <- five_min %>% 
  filter(common == "Yellowfin croaker") %>% 
  group_by(op_code, site_full, type, geog, common, five_min) %>% 
  summarise(
    maxn = max(number)
  ) %>% 
  group_by(site_full, type, geog) %>% 
  summarize(
    mean_maxn = mean(maxn),
    sd = sd(maxn), 
    se = sd(maxn)/(sqrt(n())),
    sample_size = n()
  )

```

```{r, fig.align='center', fig.width=10}
ggplot(yf_all, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Yellowfin croaker Mean MaxN", subtitle = "Averaged with each deployment as replicate") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```

```{r, fig.align='center', fig.width=10}
ggplot(yf_ten, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(limits = c(0, 7),
                     n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Yellowfin croaker Mean MaxN", subtitle = "Averaged across each 10 minute segment of 60 min deployment") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```

```{r, fig.align='center', fig.width=10}
ggplot(yf_five, aes(x = reorder(site_full, -geog), y = mean_maxn, fill = type)) +
  geom_bar(stat="identity", 
           position = "dodge")  +
  scale_fill_manual(values = wes_palette("Moonrise2")) +
  geom_errorbar(aes(x = site_full, ymax = mean_maxn + se, ymin = mean_maxn - se), 
                width = .2, 
                alpha = 0.9, 
                position = position_dodge(width = 10)) +
  scale_y_continuous(limits = c(0, 7),
                     n.breaks = 10) +
  labs(x = "Site", y = "Mean MaxN", title = "Yellowfin croaker Mean MaxN", subtitle = "Averaged across each 5 minute segment of 60 min deployment") +
  theme_minimal() +
  theme(legend.title = element_blank()) +
  coord_flip()
```